import PasswordValidationMessagePipe from './password-validation-message.pipe';

describe('PasswordValidationMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new PasswordValidationMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
