import LoginMessagePipe from './login-message.pipe';

describe('LoginMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new LoginMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
