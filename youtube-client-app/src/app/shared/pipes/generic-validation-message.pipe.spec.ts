import GenericValidationMessagePipe from './generic-validation-message.pipe';

describe('GenericValidationMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new GenericValidationMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
