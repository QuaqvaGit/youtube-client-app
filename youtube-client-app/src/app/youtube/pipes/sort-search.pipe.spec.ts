import SortSearchPipe from './sort-search.pipe';

describe('SortSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
