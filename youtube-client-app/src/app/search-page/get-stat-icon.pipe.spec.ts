import { GetStatIconPipe } from './get-stat-icon.pipe';

describe('GetStatIconPipe', () => {
  it('create an instance', () => {
    const pipe = new GetStatIconPipe();
    expect(pipe).toBeTruthy();
  });
});
